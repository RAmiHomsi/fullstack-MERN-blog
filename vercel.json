{
  "buildCommand": "cd client ; npm install ; npm run build ; npm install -g serve ; serve -s build",
  "outputDirectory": "client/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    },
    { "source": "/(.*)", "destination": "/" }
  ]
}
